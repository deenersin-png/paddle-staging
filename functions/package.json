{
  "name": "paddle-app-functions",
  "description": "Pre-run email for the Paddle App",
  "private": true,
  "type": "module",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "sync": "node scripts/sync.js",
    "check": "node scripts/check.js",
    "logs": "firebase functions:log --only preRunEmail"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "nodemailer": "^6.9.15"
  }
}
